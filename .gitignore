# Build artifacts (the .exe is shipped via GitHub Releases, not committed)
dist/
build/
*.spec

# Python
__pycache__/
*.py[cod]
*.egg-info/

# App runtime state
last_prices.json

# Web-app upload credentials (URL + shared ingest secret) — machine-specific
upload.json

# Web app runtime state & secrets
web/midea.db
web/.vapid.json
web/.vapid_private.pem

# Virtual environments
venv/
.venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
