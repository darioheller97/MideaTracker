# Web app (midea.iceatea.me) — in addition to the scraping engine deps
fastapi
uvicorn[standard]
jinja2
python-multipart
apscheduler
pywebpush
cryptography

# Shared scraping engine (same as the desktop app)
requests
PySocks          # lets the requests fallback use SCRAPER_PROXY socks5:// tunnels
lxml
cssselect
playwright
curl_cffi
